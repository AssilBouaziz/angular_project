import { Injectable } from '@angular/core';
import { GLOBAL } from 'src/app/app-config';

@Injectable({
  providedIn: 'root',
})
export class ToolService {
  // Taboutils:Outil[] = GLOBAL._DB.tools
  constructor() {}
}
